import product from './product';
import brand from './brand';
import category from './category';
import page from './page';
import siteConfig from './siteConfig';

export const schemaTypes = [product, brand, category, page, siteConfig];
